<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="subscribe">
	<select id="select" resultType="subscribeDTO">
		select subscribe_id, subscribe_date, subscribe_count, user_id, product_name from tbl_subscribe s join tbl_product p 
		on s.product_id = p.product_id and user_id = #{user_id} 
	</select>

</mapper>